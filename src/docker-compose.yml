version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=super-secret-key-for-docker-32-chars!!!
      - DATABASE_URL=sqlite:///data/notes.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped